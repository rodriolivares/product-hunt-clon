import { css } from '@emotion/react'
import React from 'react'

const Cargando = () => {
   return ( <h1
      css={css`
        margin-top: 5rem;
        text-align: center;
      `}
    >Cargando</h1>
  )
}

export default Cargando